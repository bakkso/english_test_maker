{
    "name": "english-question-generator",
    "version": "1.0.0",
    "scripts": {
      "client": "cd client && PORT=4006 npm start", // 클라이언트 실행
      "dev": "concurrently \"npm run client\"" // 동시에 클라이언트 실행
    },
    "dependencies": {
      "@anthropic-ai/sdk": "^0.24.3",
      "axios": "^0.21.1",
      "dotenv": "^16.4.5"
    },
    "devDependencies": {
      "concurrently": "^6.0.0"
    }
  }
  